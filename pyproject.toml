[project]
name = "mendelson-as2-proxy"
version = "0.1.0"
description = "REST API proxy for Mendelson AS2 XML/TCP interface"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.1",
    "python-multipart>=0.0.27",
    "uvicorn>=0.46.0",
]
